name: test-sample-availability

on:
  - push

permissions: read-all

jobs:
  setup-prerequisites:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    steps:
      - uses: ocaml/setup-ocaml@v3.2.7
        with:
          ocaml-compiler: 5.1.1
  
  

# Repository Guidelines

This repository uses the Dune build system for OCaml.

* After modifying code or documentation, run `dune build` to ensure the project compiles.
* There is no automated test suite, so a successful build is sufficient verification.
* Sample programs reside in `samples/` and can be run with `./scripts/run <sample>`.
